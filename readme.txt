Form validation with database
    User has an  __id(unique), name, branch, roll_number(unique) and phone_number

    Apis to be made:

A. Signup api                 //user api  (POST)

B. Login api                   //user api   (POST)

C. Show all users api    // admin api  (GET)

D. Delete a user api      //admin api. (DELETE)